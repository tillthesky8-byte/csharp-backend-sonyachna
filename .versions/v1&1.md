## List of improvements in v1.1 
1. Fix redundant todoId in following endpoints: *done*
   - UpdateTodo (PUT /api/todos/{id}) **done**
   - PatchTodo (PATCH /api/todos/{id}) **done**
   - MarkTodoAsFailed (PATCH /api/todos/{id}/mark-failed) **done**
   - MarkTodoAsCompleted (PATCH /api/todos/{id}/mark-completed) **done**


2. Create special class - service response, to wrap the actual data and the success status of the operation. *done*

3. Change DateTime to real unix timestamp with dt long. This is to avoid the timezone issues when using DateTime and make it easier to work with the data in the frontend. 

4. Add error layers - db error, repository error, service error, controller error. 

5. Method GetDreamEntriesByDate - change the endpoint to GET /api/dreamentries/{date} and remove the request body. *done*


## List of improvements in v1.2
1. Simplify DTOs, and pass the object directly to the service layer instead of mapping it to a new object. Move object creating to the service layer, controller should pass the data
2. Method SubmitSurvey:
    - Check for the existence of today's session.
    - Check for the existence of any questions.
    - Check for uniqness of the question ids.
    - Check for the existence of the question ids, given in the request, in the database.


3. **Major**: Add an administrative controllers for non-user functionality.
